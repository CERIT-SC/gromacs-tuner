apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-api-svc
  namespace: {{ .Release.Namespace }}
spec:
  type: ClusterIP
  ports:
    - name: gromacs-tuner-api-port
      port: 8000
      targetPort: 8000
  selector:
    app: {{ .Release.Name }}-api
